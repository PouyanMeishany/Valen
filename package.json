{
  "name": "keki-website",
  "version": "1.0.0",
  "private": true,
  "description": "Keki public landing/policy page — static, no framework.",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html dist/index.html"
  }
}
